# ─────────────────────────────────────────────
#  비밀정보 — 절대 커밋 금지 (RULES #2)
# ─────────────────────────────────────────────
wp-config.php
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.crt
secrets.json
aws_go.js
naver_login*.js
.vscode/
sftp.json
sftp.json.disabled

# 아고다/어필리에이트 토큰류 (링크 ID와 구분, 토큰은 절대 커밋 금지)
affiliate.secret.json
data/affiliate.secret.json

# API 키·계정·개인보안 파일 (TTJ 등에서 실수로 복사돼도 차단)
apikey.txt
*apikey*
*api_key*
*api-key*
*.secret
*.secret.*
*token*
*password*
*credentials*
user_info*.txt
env.dabao
env.dabao.*
!env.dabao.example
*.cookies
*.session
captcha_layer_*.png

# ─────────────────────────────────────────────
#  WordPress 스택 — 전량 폐기 (PHP 미지원, $0의 핵심)
# ─────────────────────────────────────────────
wp-admin/
wp-includes/
wp-content/
*.php
.htaccess
.sftpignore

# ─────────────────────────────────────────────
#  의존성 (재설치 가능)
# ─────────────────────────────────────────────
node_modules/
**/node_modules/
package-lock.json

# ─────────────────────────────────────────────
#  대용량 미디어 — Cloudflare Pages 25MB/파일 한도
# ─────────────────────────────────────────────
*.mp3
*.mp4
*.mov

# ─────────────────────────────────────────────
#  OS / 에디터 잡파일
# ─────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log

# ─────────────────────────────────────────────
#  내부 설계 문서 — 공개 배포 금지 (로컬 보관용)
# ─────────────────────────────────────────────
DEVELOP-PLAN.md
MIGRATION-PLAN.md
00_howcartful_css_explanation.html
